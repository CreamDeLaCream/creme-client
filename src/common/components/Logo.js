import React from 'react';

// components
import { Text } from './';

const Logo = () => {
  return (
    <div>
      <Text type="logo" color="var(--main)">
        옥자가 왜 그럴까
      </Text>
    </div>
  );
};

export default Logo;
