import React from 'react';

// components
import { Text } from './';

const Logo = () => {
  return (
    <div>
      <Text type="logo" color="var(--main)">
        오늘의 마이펫
      </Text>
    </div>
  );
};

export default Logo;
